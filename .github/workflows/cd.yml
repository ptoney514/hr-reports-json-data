name: CD Pipeline (Disabled)

# CD is disabled for this private local-only project.
# This workflow serves as documentation for what CD would do.
#
# For deployment options:
# - Vercel: Best for React apps, free tier, works with private repos
# - Netlify: Similar to Vercel
# - GitHub Pages: Requires public repo or paid plan
#
# To enable CD with Vercel:
# 1. Connect repo to Vercel at vercel.com
# 2. Vercel handles deployment automatically (no workflow needed)

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  placeholder:
    name: CD Placeholder
    runs-on: ubuntu-latest
    if: false  # Never runs - this is just documentation
    steps:
      - run: echo "CD is disabled for this project"
