# Development Dockerfile with hot reload support for JSON-based data
FROM node:18-alpine

# Set working directory
WORKDIR /app

# Copy package files first for better caching
COPY package*.json ./

# Install dependencies
# Skip the prepare script which tries to build (we're in dev mode)
RUN npm install --ignore-scripts

# Copy public directory (needed for development server and JSON data)
COPY public/ ./public/

# Create necessary directories for React components
RUN mkdir -p src/core src/hooks src/components

# Expose port 3000 for dev server
EXPOSE 3000

# Set environment variables for container-based development
ENV CHOKIDAR_USEPOLLING=true
ENV WATCHPACK_POLLING=true
ENV WDS_SOCKET_HOST=0.0.0.0
ENV WDS_SOCKET_PORT=0
ENV FAST_REFRESH=true

# Use react-scripts for hot reload
CMD ["npm", "start"]